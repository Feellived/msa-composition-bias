# 실행 순서 — 2026-08

> ⚠️ **시점 문서 (2026-08).** 당시 실행 순서 기록이다. 현재 유효한 실행 순서는 루트
> `README.md`를 볼 것. 스크립트 이름은 2026-08-12 개명 이후 이름으로 갱신돼 있다.


전제: `cd ~/projects/msa-composition-bias/pipeline && git pull`
env: `DATA=/mnt/data/msadepth` · `CD=~/projects/epitope-guided-docking/pipeline`

---

## 0. 상태 확인 (1분, CPU)

```bash
cd ~/projects/msa-composition-bias/pipeline && git pull
ls results/sites_*.json | wc -l                 # 30 이어야 함
ls $CD/results/abepiscore_all.csv               # 있어야 함
```

---

## 1. 선택기 평가 (즉시, CPU)

무엇을 하나: 후보를 고르는 규칙 7개를 **F1·Jaccard·정밀도**로 비교한다.
⚠️ 덮음(recall)만 쓰면 넓게 던진 후보가 자동으로 이긴다 — 그래서 지표를 바꿨다.

```bash
python select_eval_selectors.py --sites results \
       --abepi $CD/results/abepiscore_all.csv
```

기대: `ncomp_x_abemax` 가 F1 천장대비 0.76 으로 1위, 현재 쓰는 `ncomp` 는 0.68.

---

## 2. 선택기 교체 재도킹 (GPU 조금)

무엇을 하나: 선택기를 바꿔 후보를 다시 고르고, **ncomp 와 달라지는 타깃만** 네 팔로 재실행.

```bash
bash run_reselect.sh                 # 먼저 무엇이 바뀌는지만 본다
bash run_reselect.sh --apply         # 실제 실행
```

산출: `$CD/results/demo_dockq_reselect.csv`
비교: 기존 `demo_dockq.csv` 와 나란히 놓고 팔별 값이 오르는지 본다.

---

## 3. Boltz-2 조성 재현 (GPU, 5종)

질문: 조성 효과가 **Protenix 하나에서만 나는 것인가.**
⚠️ 2026-07-27 a3m 사고로 boltz 는 MSA 를 제대로 넣어 본 적이 한 번도 없다.

### 3-1. 게이트 (건너뛰지 말 것)

```bash
python prep_a3m_check_match.py --only "8k3k_D 8ulr_HL 8sis_HL 8k46_I 8tp5_HL"
```

`정상` 이 아닌 항목이 하나라도 있으면 **여기서 멈추고** 먼저 고친다:

```bash
python prep_a3m_fix_query.py --only "..."           # 기본 dry-run
python prep_a3m_fix_query.py --only "..." --apply
```

### 3-2. 실행

```bash
CSV=maintest.csv bash run_maintest_boltz.sh --apply \
    --only "8k3k_D 8ulr_HL 8sis_HL 8k46_I 8tp5_HL"
```

### 3-3. 실행 후 확인 (필수)

boltz 는 MSA 를 버려도 **경고만 남기고 정상 종료**한다. 반드시 본다.

```bash
grep -i -E "msa|warn|ignor" $DATA/logs/*.log | tail -40
```

### 3-4. 채점

```bash
for t in 8k3k_D 8ulr_HL 8sis_HL 8k46_I 8tp5_HL; do
  REDO=1 bash run_analyze_target.sh "$t"
done
python analyze_anchor_test.py --only "8k3k_D 8ulr_HL 8sis_HL 8k46_I 8tp5_HL" --perms 200000
```

판정: 다섯 종에서 순열 검정이 **같은 방향으로 유의**하면 모델 일반화가 선다.

---

## 4. ENR-2 확장 (1·3 결과 확인 후)

`plan/enrich_prereg.md` 의 빈칸 두 개를 **먼저** 채운다.

- Neff80 하한
- 모델별 학습 컷오프 (Protenix 버전 · Boltz-2) → `plan/MODEL_CUTOFFS.md` 로 남길 것

⚠️ 후보를 뽑기 **전에** 커밋해서 타임스탬프를 남긴다.

---

## 안전 규칙

- 설치·삭제·대량 작업은 **실행 전 확인**
- 내 폴더 밖은 건드리지 않는다
- 토큰·비밀번호는 **채팅·문서·깃에 절대 남기지 않는다**
